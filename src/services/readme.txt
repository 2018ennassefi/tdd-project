Business logique -> Les validateurs, fonctions qui aggregent des recettes selon les ingrédients.


Example : 
const createUser = () => {
    //appels dans src/data
    //valide les arguments
    // calcule à faire genre aggregent
    return User 
}
